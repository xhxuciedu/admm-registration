# JMIV consistency-check report

Generated during final packaging on 2026-08-10 from the tracked result files
and the submission build script.

## Source/result checks

- FIRE source: `results/real2d_v1/fire256_final_clean.csv`.
- FIRE summary: `results/real2d_v1/fire256_summary.json`.
- FIRE coverage: 134 pairs, five methods per pair.
- Four-corner versus externally validation-selected fixed `(0.1,1.0)`: median
  total time 4.774 s versus 6.680 s; median pairwise runtime improvement
  34.36% (bootstrap 95% CI 30.73--35.69%); median pairwise iteration
  improvement 39.56%.
- Four-corner versus safeguarded BB proxy: 51.67% median pairwise runtime
  improvement; four-corner was faster on 94.03% of pairs.
- FIRE median TRE difference versus external fixed: `-1.40e-5` pixels;
  maximum absolute difference `3.90e-4` pixels; no nonpositive Jacobians for
  any compared method.
- One-shot/pyramid-level ablation: 18 balanced FIRE pairs; one-shot 2.651 s
  and 243 median iterations, per-level 3.895 s and 222 median iterations.
- CIMA summary source: `results/real2d_v1/cima_summary.json`.

## Build checks

- `submission/JMIV/main/JMIV_main.tex` is the editorial source of truth. It is
  maintained by hand and is no longer regenerated from `paper/paper.tex` by
  `scripts/prepare_jmiv_submission.py`; running that script would overwrite the
  edited manuscript.
- Main article compiled three times with `pdflatex` using the copied current
  official Springer `sn-jnl.cls`: 17 pages, no errors, no undefined controls,
  no undefined references or citations, and zero overfull boxes.
- The flat `JMIV_main_source.zip` was extracted to a new temporary directory
  and compiled there to a byte-identical 17-page result.
- Every `\ref`/`\eqref` resolves to a defined label; every `\cite` resolves to a
  `\bibitem`; no bibliography entry is left uncited.
- All 17 rendered pages were visually inspected.
- Figure 1 was regenerated by `experiments/plot_publication_figures.py` with
  (a)--(c) panel labels and the manuscript's spectral-radius notation; the
  plotted data are unchanged.

## Notes

The Springer class emits bookmark-level and underfull-page warnings for the
long source; these are cosmetic vertical-spacing notices from the two-column
float placement. The official JMIV guideline requests `[iicol]`, and the
current official SN Article Template v3.1 implements it; the submission source
uses `pdflatex,iicol,sn-mathphys-num`. The unused `cleveref` package was
removed from the preamble.
